name: smile_meter_flutter
description: Smile detection app with ML Kit.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+2
  google_ml_kit: ^0.14.0
  provider: ^6.0.5
  flutter_svg: ^2.0.7
  cupertino_icons: ^1.0.2
  path_provider: ^2.0.15
  image: ^4.0.17
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/